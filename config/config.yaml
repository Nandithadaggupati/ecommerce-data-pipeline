database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: ${DB_NAME}
  user: ${DB_USER}
  password: ${DB_PASSWORD}

generation:
  num_customers: 1000
  num_products: 500
  num_transactions: 10000
  date_start: "2024-01-01"
  date_end: "2024-12-31"

pipeline:
  batch_size: 1000
  log_level: "INFO"
